<!doctype html>
<html lang="en">
<head>
    <title>six studio</title>
    <meta name="description" content="" />

    {{>head}}
</head>

<body>
    <div class="blocks-page">
        {{>header2}}

        {{>hero}}
        {{>features}}
        {{>gallery}}
        {{>pricing}}
        {{>testimonial}}
        {{>faq}}
        {{>service}}
        {{>posts}}
        {{>contact}}

        {{>footer}}
    </div>

    {{>scripts}}
</body>
</html>
